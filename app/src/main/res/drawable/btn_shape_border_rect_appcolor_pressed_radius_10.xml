<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
     <!-- 填充颜色 -->
    <solid android:color="@color/transparent"/>
    <!-- 线的宽度，颜色灰色 -->
    <stroke android:width="@dimen/px1" android:color="@color/app_color_pressed"/>
    <!-- 矩形的圆角半径 -->
    <corners android:topLeftRadius="@dimen/px10"
        android:bottomLeftRadius="@dimen/px10"
        android:topRightRadius="@dimen/px10"
        android:bottomRightRadius="@dimen/px10"
        />

</shape>
